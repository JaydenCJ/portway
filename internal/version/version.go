// Package version holds the single source of truth for the program name
// and version string reported by --version and the smoke test.
package version

// Name is the program name as printed in logs and --version output.
const Name = "portway"

// Version is the SemVer release version; keep in sync with CHANGELOG.md.
const Version = "0.1.0"
